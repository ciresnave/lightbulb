# 2506.22443v1

TL;DR

This paper evaluates attention sparsity patterns across layers and proposes an adaptive sparsity mask that varies by depth to preserve expressivity while reducing compute.

Why it matters for lightbulb

- Direct guidance for per-layer KV paging and sparsity strategies; complements hybrid attention scheduling and `KvPager` eviction policies.

Actions

1. Add a per-layer sparsity mask prototype in `kernels` and a tool to visualize mask density by layer.
2. Add unit benchmarks to measure memory and speed savings for several mask profiles.

Acceptance criteria

- `kernels` includes a sparsity-mask prototype and a benchmark demonstrating at least 25% memory reduction with <2% quality loss on a toy task.
