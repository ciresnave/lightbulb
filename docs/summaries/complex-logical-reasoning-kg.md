# Complex Logical Reasoning over Knowledge Graphs using LLMs — summary

TL;DR

- Augments LLMs with KG retrieval and symbolic composition (joins, paths) to answer complex queries; improves faithfulness and compositionality.

Why it matters for lightbulb

- Motivates a tool-use path for KG queries in the prompt program executor with caching and verification.

Actionable next steps

- Add a KG-query tool stub and structured result handling; log tool latency and hit rates.
- Acceptance: improved accuracy on synthetic KG reasoning tasks with bounded overhead.
