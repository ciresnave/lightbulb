# 2506.04761v2

TL;DR

This paper proposes a compact technique for adaptive layer selection conditioned on task difficulty, combining quick shallow passes with selective deep refinement. Results show strong efficiency gains on multi-step reasoning tasks.

Why it matters for lightbulb

- Reinforces our early-exit and test-time depth adaptation direction (CoLa / Skip-a-layer); provides experimental thresholds to tune scheduler patience.

Actions

1. Add a CoLa-style scheduler experiment to `tests/` that simulates shallow-then-deep inference with a toy model.
2. Add parameters to `Scheduler` to expose patience and refinement thresholds used in the paper.

Acceptance criteria

- `Scheduler` has configurable `patience` and `refine_threshold` with a unit test showing >20% token savings and <2% accuracy drop on the toy reasoning task.
