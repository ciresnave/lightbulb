# 2506.16500v1 — Sparse Mixture Routing Analysis (duplicate/variant)

TL;DR

Variant copy analyzing sparse mixture routing, load imbalance causes, and pragmatic mitigations (bucketing, capacity limits, rebalancing).

Why it matters

- Provides alternate experiments and parameter sweeps useful for robust router defaults.

Key technical takeaways

1. Slight differences in token distributions change optimal capacity settings dramatically.
2. Rebalancing policies that are too aggressive add routing compute; lightweight heuristics work best in practice.

Implementation steps for lightbulb

- Retain the variant as a companion note; extract parameter sweeps for router defaults.

Acceptance criteria

- Router's default capacity settings informed by the variant produce stable throughput on synthetic workloads.
