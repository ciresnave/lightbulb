# 2506.16500v1 (variant)

TL;DR

This variant of 2506.16500 explores scalable sparse mixture strategies with deterministic routing fallbacks to avoid stragglers under load.

Why it matters for lightbulb

- Provides pragmatic fallbacks for MoE routing under capacity pressure — useful for `Scheduler` + `KvPager` capacity-aware designs.

Actions

1. Add an MoE fallback policy example in `engine` that selects deterministic fallback experts when capacity is saturated.
2. Add a short integration test that simulates capacity saturation and validates deterministic fallback behavior.

Acceptance criteria

- A fallback routing policy is documented and a unit/integration test demonstrates no straggler >200ms on the simulated workload.
