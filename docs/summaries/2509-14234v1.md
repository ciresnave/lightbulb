# 2509.14234v1 — scheduler preemption + fairness heuristics (stub)

TL;DR

- Short summary: additional scheduling heuristics combining preemption with fairness constraints for multi-tenant CPU runtimes.

Why it matters

- Ensuring fairness across tenants while preserving tail latency requires careful scheduling heuristics.

Key takeaways

- A mixture of priority classes and adaptive preemption reduces tail-latency without starving low-priority tenants.

Implementation steps

1. Extend scheduler with priority classes and preemption hooks.
2. Add metrics and fairness checks in the runtime.

Acceptance criteria

- Improved fairness metrics and reduced tail-latency in multi-tenant experiments.
