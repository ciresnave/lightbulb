# 2510.05949v1 — per-core partitioning & prefetch for small-batch CPU inference (stub)

TL;DR

- Short summary: techniques for partitioning work across cores and prefetch strategies to reduce small-batch latency.

Why it matters

- Proper partitioning and prefetching reduce contention and tail latency on multi-core CPUs.

Key takeaways

- Lightweight partitioning rules and coordinated prefetch can improve latency stability.

Implementation steps

1. Add partitioning policies to scheduler and prefetch hints in kernels.
2. Benchmark under representative small-batch workloads.

Acceptance criteria

- Reduced 95/99 latency and improved CPU utilization.
