
# 2508.20978v1 — Per-Core Power-Aware Tiling & Dynamic Routing for Conditional Compute

## Part 1: Per-Core Power-Aware Tiling for CPU Efficiency

TL;DR

- Methods for selecting tile sizes and micro-kernel parameters per core to maximize perf per watt.

Why it matters

- On heterogeneous CPU systems, per-core characteristics vary; tuning per-core config improves sustained throughput and efficiency.

Key takeaways

- Use microbenchmarks at startup to pick conservative tiling options per core.
- Allow dynamic fallback if power governor changes.

Actions / Implementation steps for lightbulb

1. Implement a tiny per-core capability probe during startup.
2. Store per-core tiling parameters and expose overrides in config.

Acceptance criteria

- Sustained throughput/watt improves on mixed-core targets in the CI microbenchmark suite.

---

## Part 2: Dynamic Routing Strategies for Conditional Compute in Transformer Layers

TL;DR

2508.20978v1 explores dynamic routing strategies for conditional compute inside transformer layers, offering gating strategies and evaluation metrics.

Why it matters

- Helps design routers and scheduler gating thresholds for compute savings.

Actions

1. Add to Router research bucket and create gating simulation scripts.

Acceptance criteria

- Prototype demonstrating compute savings on synthetic bursty datasets.
