# 2509.02350v1 — Mixed-precision scheduling & selective attention caching (stub)

TL;DR

- Short summary: strategies to schedule mixed-precision execution across layers and selectively cache attention keys/values to reduce recompute.

Why it matters

- Mixed precision and selective caching reduce compute and memory pressure, useful for CPU-only inference.

Key takeaways

- Layer-wise mixed precision scheduling plus an eviction-aware attention cache yields good throughput/accuracy tradeoffs.

Implementation steps

1. Add an engine-level policy to choose per-layer precision and an attention cache with eviction hints.
2. Evaluate throughput and accuracy across model families.

Acceptance criteria

- Accurate per-layer precision profiles and positive throughput gains on benchmark workloads.
