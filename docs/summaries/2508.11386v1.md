# 2508.11386v1

TL;DR

This short paper analyzes adaptive attention sparsity trade-offs for medium-length sequences and reports practical heuristics for choosing block sizes and sparsity patterns that maintain quality.

Why it matters for lightbulb

- Provides concrete parameter choices for blocked-sparse and hybrid attention kernels we plan to prototype in `kernels::cpu` and `kernels::hybrid`.

Actions

1. Add the paper's recommended block sizes to `docs/benchmarks/attention_blocking.md`.
2. Run a small microbenchmark in `bench/` comparing block sizes at 256/512/1024 tokens.

Acceptance criteria

- `docs/benchmarks/attention_blocking.md` added and a benchmark shows recommended block size yields best speed/quality trade-off on a toy workload.
