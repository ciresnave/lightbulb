# 2510.04871v1 — Per-core int4/int8 kernel profiling (stub)

TL;DR

- Short summary: per-core kernel profiling for int4/int8 to select per-core defaults and layouts.

Why it matters

- Per-core profiling helps select optimal kernels and settings per physical core for heterogeneous CPUs.

Key takeaways

- Small microbenchmarks per core at startup can capture helpful priors for kernel selection.

Implementation steps

1. Implement per-core microbenchmarks and save profiles.
2. Use profiles to choose tile sizes and accumulation strategies.

Acceptance criteria

- Improved per-core throughput in mixed-core CI setups.
