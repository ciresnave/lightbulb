# 2509.07017v1 — int8 GEMM & cache-friendly quantized kernels (stub)

TL;DR

- Short summary: int8 GEMM techniques and cache-friendly quantized kernels for CPU inference.

Why it matters

- Efficient int8 GEMM speeds quantized inference; cache-aware layouts reduce memory stalls.

Key takeaways

- Blocked GEMM with quantization-aware accumulation provides significant speedups on common CPUs.
- Layouts that align with L1/L2 cache lines reduce cache thrashing for small-batch inference.

Implementation steps

1. Add int8 GEMM micro-kernel prototypes behind a feature flag.
2. Benchmark layouts and add a small auto-tuner to pick defaults.

Acceptance criteria

- Positive throughput gains on CI microbenchmarks for quantized models.
