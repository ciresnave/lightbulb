# 2509.26507v1 — Fast sparse transformer kernels (stub)

TL;DR

- Short summary: optimized sparse transformer kernels for CPU that target common sparsity patterns.

Why it matters

- Sparse kernels can dramatically reduce compute and memory for models with structured sparsity.

Key takeaways

- Blocked sparse kernels with fused quantized accumulation are effective on modern CPUs.

Implementation steps

1. Prototype sparse GEMM paths for common block sizes.
2. Integrate into kernel selection and add auto-tuning.

Acceptance criteria

- Positive throughput/accuracy trade-offs on sparse models.
