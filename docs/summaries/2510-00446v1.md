# 2510.00446v1 — Int4/Int8 mixed-precision kernel benchmarks (stub)

TL;DR

- Short summary: benchmarks and best practices for int4/int8 mixed-precision kernels on CPU.

Why it matters

- Lower-bit kernels save memory and compute, enabling larger models on CPU.

Key takeaways

- Mixed precision can be effective but requires careful accumulation strategies to avoid numerical issues.

Implementation steps

1. Add int4 support in micro-kernels and benchmark harness.
2. Implement accumulation fallbacks for critical ops.

Acceptance criteria

- Benchmarks show expected speedups without unacceptable accuracy loss.
