# 2508.13371v1

TL;DR

This note presents an empirical comparison of attention approximation schemes (sparse, low-rank, kernelized) on CPU backends, highlighting regimes where each approach is preferable.

Why it matters for lightbulb

- Helps choose default kernel selection heuristics for `kernels` and informs our hybrid attention scheduler thresholds.

Actions

1. Capture the empirical regimes in `docs/benchmarks/attention_kernels.md`.
2. Add a kernel-selection heuristic unit test that picks kernels by sequence length and estimated cost.

Acceptance criteria

- `docs/benchmarks/attention_kernels.md` added and unit tests demonstrate the heuristic selects the expected kernel for representative inputs.
