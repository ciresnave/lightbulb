# From Bytes to Ideas: Autoregressive U-Nets — summary

TL;DR

- Explores AR U-Net architectures as alternatives to transformers for sequence modeling; potential for structured decoding.

Why it matters for lightbulb

- Another candidate for “linear/structured” mixers; informs hybrid schedules and kernel needs.

Actionable next steps

- Track Candle feasibility; document how AR U-Nets would integrate with current decode loop abstractions.
- Acceptance: doc-only spike; if feasible, a micro-benchmark comparison.
