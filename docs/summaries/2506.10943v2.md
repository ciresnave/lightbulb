# 2506.10943v2

TL;DR

This work introduces an evaluation suite for routing overheads in modular architectures and shows lightweight routing schemes with minimal latency impact when using shallow routing networks.

Why it matters for lightbulb

- Gives concrete microbenchmarks and measurement methodology we can reuse when prototyping our `Router` and capacity-aware inference features.

Actions

1. Add microbenchmark harness `bench/routing_bench.rs` (or a unit test) measuring router latency relative to a single expert call.
2. Capture the measurement methodology in `docs/benchmarks/routing.md` for reproducible evaluation.

Acceptance criteria

- A routing microbenchmark exists and demonstrates router overhead ≤ 10% of expert inference time on a toy setup.
